/**
 * Trazo más fino que el default de Lucide (~2) — lectura más editorial,
 * menos “app de fitness”, alineada con la estética de la marca.
 */
export const lucideBrand = {
  strokeWidth: 1.4,
} as const;
